// English catalogue for the device-data page (`mdsplus.html`) only.

self.FyI18n.register('en', {
  'mds.title': 'Device data · fylite',
  'mds.h1': 'Device data',
  'mds.sub': 'Browse an MDSplus tree · name a shot · pull the signals you pick',
  'mds.lead': 'This page treats an <strong>MDSplus server</strong> as a directory you can walk into: choose a tree, give a shot number, descend to the nodes, pick a few signals and draw them. It is <strong>read-only</strong> — no entry point here writes, deletes, or evaluates an arbitrary expression.',
  'mds.bound': '<strong>The browser cannot reach MDSplus.</strong> mdsip is raw TCP, a page has no socket, and wasm does not change that (FYL-DESIGN-06 §1); so this page is served by a gateway on your own machine, and it is the GATEWAY that has to be able to reach the device, not your browser. <strong>What is drawn is every Nth sample</strong> — not a mean, not a min/max envelope. A spike narrower than the stride <strong>is not in this curve</strong>, which is what the word "decimated" in each caption means. This page is <strong>not a data repository</strong>: samples are not kept, every draw goes back to the server. To keep a shot\'s <strong>reduction</strong> together with its recipe and manifest, that is what <code>tools/mds-shot-to-fyo.py</code> is for. Whether the data itself may travel is a <strong>governance</strong> question and not one this page can answer.',

  // --- the gateway ---------------------------------------------------
  'mds.server': 'Data gateway',
  'mds.server.what': 'The page never connects to MDSplus. It asks this same-origin gateway, which uses a read-only mdsip client to reach the device.',
  'mds.gw.checking': 'Looking for the gateway…',
  'mds.gw.up': 'Gateway up. mdsip target <code>{server}</code>, username <code>{user}</code>.',
  'mds.gw.down': '<strong>No gateway.</strong> Opened as a static file this page has no data — it needs a process that can open a socket.',
  'mds.gw.howto': 'Start one at the repository root, then open this page at the address it prints:',
  'mds.gw.tunnel': 'The EAST server sits on the site network (<code>202.127.204.12:8000</code>). From a workstation, tunnel first and point the gateway at the near end:',
  'mds.server.addr': 'mdsip server',
  'mds.server.ph': 'host[:port]',
  'mds.server.use': 'Switch to it',
  'mds.server.east': 'EAST, on the site network',
  'mds.server.tunnel': 'the near end of an ssh tunnel',
  'mds.server.bad': 'Write it as <code>host</code> or <code>host:port</code>. This box takes an address and nothing else is sent.',
  'mds.server.set': 'Target is now <code>{server}</code>. The tree, the shot and the traces are cleared — open again before fetching, because <strong>the same shot number on another device is not the same shot</strong>.',
  'mds.server.free': 'The gateway is bound to loopback, so this box is open: it can be pointed at any mdsip server <strong>the gateway</strong> can reach. (The username is not set here — mdsip does not authenticate, so a username the page chose would prove nothing.)',
  'mds.server.locked': 'This gateway is <strong>not bound to loopback</strong>, so it only accepts the servers named when it was started: <code>{list}</code>. Anything else needs the gateway restarted.',

  // --- shot ----------------------------------------------------------
  'mds.pick.shot': 'Tree and shot',
  'mds.tree': 'Tree',
  'mds.shot': 'Shot',
  'mds.open': 'Open',
  'mds.opening': 'Opening {tree} #{shot} …',
  'mds.opened': 'Opened {tree} #{shot}; {n} entries at the top.',
  'mds.shot.bad': 'A shot number is an integer.',
  'mds.openfail': '<strong>#{shot} cannot be opened on {tree}</strong> — the reason is in the browser note below, in the server\'s own words. Other trees may still have it: every picked signal asks for itself and each caption says what came back.',

  // --- shot navigation ------------------------------------------------
  'mds.shot.prev': 'Prev',
  'mds.shot.next': 'Next',
  'mds.shot.latest': 'Latest',
  'mds.shot.what': '<strong>Prev / Next move exactly one shot, and never skip an empty one for you.</strong> A shot number is a site counter, not an arithmetic sequence, and the gaps in it are a fact about the site: the page <strong>reports</strong> what it finds — the tree says it cannot be opened, each picked signal says whether it has anything on that shot — and the number stays there, so pressing again steps past it. <strong>Latest is the only button here that asks the server</strong>: it reads <code>current_shot(tree)</code>, the one number this page cannot work out (and the one in UDAClient\'s status line — this repo compared them digit for digit).',
  'mds.shot.stepped': 'Shot #{from} → <strong>#{shot}</strong>. The traces and the node listing are cleared — the same node on another shot is another discharge.',
  'mds.latest.asking': 'Asking {tree} for its latest shot …',
  'mds.latest.got': 'The latest shot on {tree} is <strong>#{shot}</strong> (<code>{expr}</code>). ★That number says how far the site has counted; it does <strong>not</strong> promise the shot holds data — the one being written usually does not yet.',
  'mds.latest.fail': 'Could not ask {tree} for its latest shot: {why}',
  'mds.latest.notree': 'Pick a tree first — <code>current_shot</code> counts the shots of <strong>one tree</strong>, not "of this device".',

  // --- the zero-order quantities --------------------------------------
  'mds.zero': 'Zero-order quantities',
  'mds.zero.what': 'What a shot is looked at through first. <strong>They are not in one tree</strong> — I<sub>p</sub> is the PCS Rogowski, the loop voltage is a raw channel on the device tree, W<sub>MHD</sub> is an EFIT result — so each one carries its own tree and none of them depends on what the browser below has open. ★<code>\\PCRL01</code> is the Rogowski reading and <strong>not "the plasma current"</strong>: this repo measured it 1.9 % from the reconstructed <code>cpasma</code> at the flat top of #137985. It is the right thing to look at first and the wrong thing to fit to.',
  'mds.zero.off': 'This gateway offers none of the EAST trees (<code>{trees}</code>), so none of these can be pressed — they are EAST names, another device calls its signals something else, and the page will not guess for you.',
  'mds.q.ip': 'plasma current I<sub>p</sub>',
  'mds.q.vloop': 'loop voltage V<sub>loop</sub>',
  'mds.q.wmhd': 'stored energy W<sub>MHD</sub>',
  'mds.q.ne': 'line-averaged density n̄<sub>e</sub>',
  'mds.q.betap': 'poloidal beta β<sub>p</sub>',
  'mds.q.li': 'internal inductance l<sub>i</sub>',
  'mds.q.q95': 'safety factor q<sub>95</sub>',

  // --- overlaying shots (T-S3) ----------------------------------------
  'mds.pin': 'Overlay this shot',
  'mds.unpin': 'Only the current one',
  'mds.pin.what': '<strong>Pin</strong> the current shot: it stays on the figures after the page moves to another shot, so the same channel is drawn twice, side by side. ★Pinning issues <strong>no request</strong> — what is pinned is what has <strong>already come back</strong>. The current shot is solid, a pinned one dashed, and both the legend and the caption carry the shot number.',
  'mds.pin.on': 'Pinned: {shots}; current #{cur}. Every trace is drawn for all of them, and the readings table has a row per shot.',
  'mds.pin.none': 'Nothing pinned any more — only the current shot.',
  'mds.pin.noshot': 'Give a shot number first — there is nothing to pin.',
  'mds.pin.already': '#{shot} is already pinned.',
  'mds.pin.limit': 'At most {n} shots pinned at once: beyond that the legend takes more room than the curves.',
  'mds.pin.drop': 'drop #{shot}',

  // --- the status line (T-S1) ------------------------------------------
  'mds.status': 'Status line',
  'mds.status.what': '★This is not "six numbers", it is <strong>where each of them comes from</strong>: every field names the tree and node it was read from, and the derived ones name the rule too. ★<code>I<sub>t</sub></code> (TF coil current) and the date <strong>used to be blank</strong> — this repository did not know which nodes held them and would not fill the slots with something close; on 2026-08-24 both were swept out of the live tree: <code>east:\\TOP.T2:TFP</code> (amperes, 10.7 kA on #165704) and <code>getnci(…,"TIME_INSERTED")</code> on a data node. ★The <strong>vacuum toroidal field B<sub>t</sub></strong> keeps its own row — it is not I<sub>t</sub>, they are two quantities.',
  'mds.st.shot': 'Shot',
  'mds.st.ip': 'Current I<sub>p</sub>',
  'mds.st.pulse': 'Pulse length',
  'mds.st.bt': 'Toroidal field B<sub>t</sub>',
  'mds.st.date': 'Date',
  'mds.st.it': 'TF coil current I<sub>t</sub>',
  'mds.st.from.it': 'from <code>{node}</code>, the sample of largest magnitude ({t} {u}). ★This node was swept out of the live tree on 2026-08-24: of the TF family under <code>\\TOP.T2</code> it is the one in <strong>amperes</strong> that is flat across the shot (10.7 kA on #165704); <code>\\TOP.T1</code>\'s <code>ITF05…ITF16</code> read ~0.02 A and are not the supply current.',
  'mds.st.from.written': 'from <code>getnci(…,"TIME_INSERTED")</code> on <code>{node}</code>. ★This is when that <strong>record was written</strong>, not when the plasma was made — minutes apart for a raw acquisition channel, possibly days for a re-analysed result. (<code>\\TOP</code> itself answers 0: the root carries no record, so the question is asked of a node that holds data.)',
  'mds.st.nodate': 'Nothing came back for this shot yet, so there is no record to ask when it was written.',
  'mds.q.it': 'TF coil current I<sub>t</sub>',
  'mds.st.from.page': "the page itself: tree <code>{tree}</code> plus the shot box (from <code>current_shot</code> when Latest was pressed).",
  'mds.st.noshot': 'No shot yet.',
  'mds.st.pick': 'fetch <code>{node}</code> and this field fills in.',
  'mds.st.add': 'add it',
  'mds.st.from.ip': 'from <code>{node}</code>, the <strong>sample of largest magnitude</strong> ({t} {u}). ★It is the Rogowski reading and <strong>not</strong> the plasma current (measured 1.9 % from the reconstructed <code>cpasma</code> at the flat top of #137985); and the samples are <strong>decimated</strong>, so the true peak may fall between two of them.',
  'mds.st.from.bt': "from <code>{node}</code> (EFIT's vacuum toroidal field), the sample of largest magnitude ({t} {u}).",
  'mds.st.from.pulse': 'derived from <code>{node}</code>: the stretch where |I<sub>p</sub>| ≥ {pct} % of its peak ({t0} … {t1} {u}). ★<strong>That rule is this page\'s own</strong>, not the site\'s definition of a pulse.',
  'mds.st.nopulse': '<code>{node}</code> carries no time base here, so no length can be derived.',

  // --- reading and panning on the figure (T-S2) -------------------------
  'mds.read.one': '{label} @ {t} {xu} = {y} {yu}',
  'mds.pan.nowin': 'The window is the whole shot, so there is nothing to slide — drag out a stretch first.',
  'mds.pan.clock': 'This trace is on a different clock from the one the window was drawn on; it cannot be slid.',
  'mds.pan.live': 'on release: {t0} … {t1}',
  'mds.png': 'Save PNG',

  // --- the workspace (T-S4) --------------------------------------------
  'mds.ws': 'Workspace',
  'mds.ws.what': 'Save <strong>what the page is set to</strong> as one JSON file: server, tree, shot, pinned shots, picked signals, columns, sampling, window. ★<strong>No samples</strong> — this page is not a data repository (see the top of the page); a workspace carrying arrays would be a copy of the shot with no provenance. Loading it goes back to the server for the data.',
  'mds.ws.save': 'Save workspace',
  'mds.ws.load': 'Load workspace',
  'mds.ws.saved': 'Workspace saved ({n} picked signals; no samples in the file).',
  'mds.ws.loaded': 'Workspace loaded: {n} signals, {shots} pinned shots, {dropped} entries dropped as malformed. Fetching the shots the file names.',
  'mds.ws.bad': 'This is not JSON that parses: {why}',
  'mds.ws.nottype': 'This is not a workspace file (<code>@type</code> = {type}; expected <code>fylite:MdsWorkspace/1</code>).',
  'mds.col.shot': 'Shot',
  'mds.q.bt': 'toroidal field B<sub>t</sub>',

  // --- browsing ------------------------------------------------------
  'mds.browse': 'Tree',
  'mds.browse.what': 'Structures open one level at a time; the leaves are the nodes that hold data. <strong>Bytes</strong> is the size of the RECORD stored at that node: zero certainly cannot be read, and non-zero <strong>still may not be</strong> (the record can be an expression pointing somewhere this shot has nothing). When it cannot be read you get the error on the right, not an empty curve.',
  'mds.up': 'Up',
  'mds.filter': 'Filter',
  'mds.filter.ph': 'by name, e.g. POINT_F',
  'mds.col.name': 'Name',
  'mds.col.usage': 'Kind',
  'mds.col.bytes': 'Bytes',
  'mds.empty': 'This level is empty.',
  'mds.loading': 'Listing {path} …',
  'mds.listed': '{path}: {n} entries ({shown} shown).',
  'mds.big': '{n} entries at this level — type a few letters in the filter above.',
  'mds.walkfail': 'Cannot list {path}: {why}',

  // --- selection and fetch -------------------------------------------
  'mds.direct': 'Node path, typed',
  'mds.direct.what': '★The names most used on EAST are <strong>tags</strong> (<code>\\POINT_F1</code>, <code>\\PCRL01</code>), and a tag does not hang in the structure — no amount of walking reaches it. If you know the name, write it here; it is taken in the tree selected above.',
  'mds.direct.ph': '\\POINT_F1',
  'mds.add': 'Add',
  'mds.direct.bad': 'Node paths only: letters, digits, <code>_ $ \\ . : -</code>. Anything with brackets, quotes, commas or spaces is not sent.',
  'mds.direct.notree': 'Open a tree first — a typed path only means something inside one.',

  'mds.picked': 'Selected signals',
  'mds.picked.none': 'Nothing selected. Press a zero-order quantity above, or click a node that holds data in the browser.',
  'mds.limit': '{n} at a time — drop one before adding another.',
  'mds.clear': 'Drop all',
  'mds.remove': 'Drop',

  // --- figures, sampling and the window -------------------------------
  'mds.figs': 'Time traces',
  'mds.figs.none': 'No traces yet. Pick a few signals on the left, then press Fetch.',
  'mds.fetch': 'Fetch',
  'mds.fetching': 'Fetching {node} …',
  'mds.refining': 'Refining {node} to the figure width …',
  'mds.got': '{n} traces fetched.',
  'mds.got.refined': '{n} traces fetched; {refined} of them refined to about {points} points each.',
  'mds.cols': 'Columns',
  'mds.cols.auto': 'adaptive',
  'mds.cols.n': '{n} columns',
  'mds.cols.now': 'Layout: {cols} column(s), {n} figures. A narrow screen (&lt; 900 px wide) falls back to one column whatever is selected.',
  'mds.got.none': '<strong>Nothing came back for this shot.</strong> All {n} traces failed on #{shot}; each figure below carries the server\'s own words for why. The shot number stays here — press Prev / Next again to step past it.',
  'mds.rate': 'Sampling',
  'mds.rate.auto': 'adaptive (figure width)',
  'mds.rate.n': '{n} points/trace',
  'mds.rate.what': '<strong>Two passes.</strong> The first asks for at most <strong>256 points</strong> per trace and lays out the whole shot in seconds; the second refines each trace to the figure\'s own <strong>pixel width</strong>, which is as much as a line can show. <strong>Drag across a figure</strong> and both passes run again inside that window: the point count barely changes, the <strong>stride collapses</strong> — the sampling rate follows the window instead of the shot. Double-click to go back to the whole shot. The stride is always in the caption.',
  'mds.win.all': 'Whole shot',
  'mds.win.none': 'Window: the whole shot. Drag across any figure to zoom into a stretch of time; double-click to come back.',
  'mds.win.on': 'Window: {t0} … {t1} {u} (double-click any figure for the whole shot).',
  'mds.win.skip': '★This trace is on a {u} axis, not the clock the window was drawn on — it is still the whole shot.',
  'mds.win.outside': '★This window falls outside this node\'s samples; what is drawn is still the previous fetch.',

  'mds.cap.dec': '{node} — {n} samples, <strong>every {stride}th taken</strong>, {shown} drawn. This is decimation, not reduction.',
  'mds.cap.full': '{node} — {n} samples, all of them.',
  'mds.cap.window': 'Window = samples {first}…{last} ({t0}…{t1} {u}).',
  'mds.cap.fail': '{node} — <strong>cannot be read</strong>: {why}',
  'mds.axis.t': 'time [{u}]',
  'mds.axis.i': 'sample index',
  'mds.table': 'Readings',
  'mds.col.tree': 'Tree',
  'mds.col.node': 'Node',
  'mds.col.units': 'Units',
  'mds.col.n': 'Samples',
  'mds.col.stride': 'Stride',
  'mds.col.min': 'Min',
  'mds.col.max': 'Max',
  'mds.col.span': 'Time span',
  'mds.export': 'Export JSON',
  'mds.saved': '{n} traces exported (saved locally, not uploaded).',
  'mds.nothing': 'Nothing to export yet.',

  // --- the diagnostic catalogue (assets/mds-catalog.js) ------------------
  'mds.cat': 'By diagnostic',
  'mds.cat.what': '★A node path does not say which tree it is on, and that <strong>cannot be derived</strong> — `\\PCRL01` is on `pcs_east`, `\\POINT_F1` on `east`, core Thomson answers on both `analysis` and `ts_east`; asking `east` for `\\PCRL01` gets you "absent", not "empty". This catalogue (an EAST Wiki harvest: 10 classes, 73 diagnostics, 376 signals) IS that mapping: <strong>pick the instrument, the catalogue supplies the tree</strong>.',
  'mds.cat.class': 'Class',
  'mds.cat.all': 'All classes',
  'mds.cat.find': 'Find',
  'mds.cat.find.ph': 'instrument, signal name or purpose',
  'mds.cat.at': 'at {where}',
  'mds.cat.rate': 'sampled {rate}',
  'mds.cat.add': 'Add',
  'mds.cat.added': 'Added {tree}:{node}.',
  'mds.cat.noshot': 'Open a shot above first — without one there is nothing to fetch.',
  'mds.cat.notree': 'private DAQ',
  'mds.cat.none': 'No diagnostic matches.',
  'mds.cat.count': '{d} of {s} diagnostics.',
  'mds.cat.clip': 'Only the first {n} rows are drawn — type a few letters above to narrow it.',
  'mds.cat.filtered': 'Showing {n} of {all} signals that match what you typed; clear "Find" for all of them.',
  'mds.cat.pattern': 'This instrument also records signals given only as a name pattern (the upstream marks it an <strong>approximate grouping</strong>, so it is not expanded into clickable rows): {pat}',
  'mds.cat.loading': 'Reading the diagnostic catalogue…',
  'mds.cat.failed': 'The diagnostic catalogue did not load: {why}. The page still works — the tree browser and the typed path are unaffected; you just cannot pick by instrument.',
});
