// English catalogue for the machine-design page's own chrome: the control the
// two function bars share.  Each bar keeps its own catalogue — the design
// bar's keys are in the shared `lang-en.js` (`design.*`), the 0-D bar's in
// `lang-zerod-*`.  What is here belongs to the page.

self.FyI18n.register('en', {
  'd.shared': 'This shot (read by all three bars)',
  'd.shared.r0': 'Major radius R<sub>0</sub> [m]',
  'd.shared.note': '★<strong>One quantity, one control</strong>: the flat-top plasma current and the target geometry R<sub>0</sub>/a/κ/δ are read by all three bars — the 0-D bar takes the current as the plateau of its waveform and the geometry as the volume its operating-domain criteria are judged on, the design bar takes them as the target it inverts for, the pulse bar scales the same geometry by phase. They sit <strong>outside</strong> the bars: folding one collapses its panels, and a shared control living inside it would take the other bar input out of sight too. ★The geometry used to be one copy per bar: seeded from the same descriptor at load (measured: a = 0.45 against 0.445) and free to drift from that moment on, so the page judged one plasma runnable and designed coils for another. Each bar has its own run key; press the one you want.',

  // --- 功能栏 pulse: the feed-forward trajectory ---------------------------
  'nav.pulse': 'Pulse trajectory',
  'pulse.caveat': 'This bar answers: <strong>what current and voltage does each supply have to deliver to walk this configuration trajectory</strong>. Each waypoint is designed by a linear isoflux solve for the coil currents; the per-channel voltages then come from the implicit Euler that is the <strong>exact inverse</strong> of this repository\'s circuit integrator, together with the currents induced in the passive structure. ★<strong>These are not per-instant equilibria</strong>: a waypoint is a state that ASKS for its configuration, not a solved one. For the configuration actually obtained at an instant, use “forward verification” below — it costs an equilibrium solve each, and the bill is stated before you pay it.',
  'pulse.grid': 'Trajectory',
  'pulse.t_breakdown': 'Breakdown [s]',
  'pulse.t_rampup': 'Ramp-up ends [s]',
  'pulse.t_flattop': 'Flat top ends [s]',
  'pulse.t_end': 'Discharge ends [s]',
  'pulse.npts': 'Waypoints',
  'pulse.a0': 'Starting minor radius a₀/a',
  'pulse.grid_note': '★The waypoints are <strong>refined by phase</strong>, not spread evenly over [0, T]. Even spacing spends the budget on the segment that does not move: measured, the default 21 waypoints on a 0–10 s pulse land every 0.5 s, which puts <strong>exactly one of them inside the 1 s ramp</strong> and fifteen on the flat top — and the ramp is where dI/dt, and so the per-turn voltage (a difference quotient across neighbouring waypoints), is largest. The grid now <strong>equidistributes arc length</strong> of the trajectory in the normalised (t/T, f) plane, f being the phase fraction the shape and the current are interpolated on (0 → 1 across the ramp, flat at 1, 1 → 0 on the way down): a phase in which the design does not move earns points for its duration alone, one that traverses the whole swing earns them for the swing too. Every phase with a duration gets at least 3 — fewer than three leaves no interior difference in it. Phase boundaries are always waypoints (they are the corners of the trapezoid), and breakdown is always exactly one (I<sub>p</sub> = 0 there, so there is no plasma to design for).',
  'pulse.gridsplit': 'At the current settings: <strong>{n}</strong> waypoints — breakdown {bd} · ramp <strong>{up}</strong> · flat top {fl} · ramp-down {dn}.',
  'pulse.keys': 'Target shapes at several instants',
  'pulse.keys_note': '★Real pulse design gives a target shape at each of a handful of <strong>key instants</strong>, rather than one flat-top shape scaled by phase. Each row here is a <strong>correction pinned at its instant</strong>: with no rows the shape is the phase-scaled flat-top target above and nothing else — <strong>bit for bit what this bar gave before</strong>; with rows, the shape at a row\'s instant <strong>is that row</strong>, and the phase structure still carries it between rows. A row\'s instant is <strong>inserted into the waypoint grid</strong> (an instant asked for is an instant designed for), it is preferred by the forward verification below, and each row is compared against <strong>its own</strong> target there. A new row is seeded with the shape the trajectory already had at that instant, so <strong>adding one and leaving it alone changes nothing</strong>.',
  'pulse.key.add': 'Add an instant',
  'pulse.key.del': 'Delete',
  'pulse.key.none': 'No instants given — the single flat-top target above is scaled by phase.',
  'pulse.key.count': '{n} instants given.',
  'pulse.key.dropped': '{m} instants usable; <strong>{n} rows fall outside (breakdown, end] or carry an invalid number and are not used</strong> — neither clipped nor replaced.',
  'pulse.shape_note': 'The configuration is interpolated by phase: after breakdown a small cross-section of a₀ grows with the I<sub>p</sub> waveform into the flat-top target (taken from the “target configuration” panel as it stands), holds through the flat top, and retraces on the way down. The current waveform is the same trapezoid rule the 0-D bar uses.',
  // --- T-D16: this bar's own session file -----------------------------------
  'pulse.j.note_hint': '★This bar has a <strong>session file of its own</strong> (toolbar “Export” → “JSON session · Pulse trajectory”): the trajectory settings above, the verification settings, the voltage limit and <strong>every row of the multi-instant target table</strong> are in it, written out verbatim, so importing it back gives the same trajectory. ★The file carries <strong>inputs</strong> and no results — a case under <code>app/cases/</code> is exactly such a session document, so the one you save for yourself is itself a case. ★What it <strong>cannot carry</strong> is the flat-top configuration this trajectory is designed toward: that is not this bar\'s input to give, it is what the “configuration and coil currents” bar <strong>solved</strong> (T-D4). The file records the one that stood at export time as provenance and <strong>never applies it</strong>; the import says to your face whether the configuration on this page is that same one.',
  'pulse.j.export_hint': 'Export this bar\'s inputs as a JSON session (multi-instant targets included; no solved configuration, no results)',
  'pulse.j.import_hint': 'Import this bar\'s JSON session (inputs only; it does not start a run)',
  'pulse.j.imported': 'Imported {name}: {n} settings, {k} usable instants{skipped}; the configuration this trajectory aims at is not in the file — the one this page solved is used.',
  'pulse.j.rows': '{n} multi-instant target rows',
  'pulse.j.norows': 'the file carries no “target shapes at several instants” section, so the rows on the page are left exactly as they are (neither cleared nor read as zero rows)',
  'pulse.j.note': 'Imported <strong>{name}</strong>: {n} settings, {rows} ({k} of the instants usable).',
  'pulse.j.src.same': 'The configuration the file was designed against is the <strong>same</strong> one this page has solved ({page}), so pressing this bar\'s run key reproduces the per-channel demand <strong>bit for bit</strong>.',
  'pulse.j.src.diff': '★The configuration the file was designed against is <strong>not</strong> the one this page has solved: file {file}, I<sub>p</sub> {fip} kA; page {page}, I<sub>p</sub> {pip} kA. The trajectory will be rebuilt on <strong>the page\'s</strong> configuration — the shape is not in the session file and is not restored from it.',
  'pulse.j.src.unsolved': '★This page <strong>has not solved a configuration yet</strong>, and the solved one is exactly what this trajectory aims at: press the run key on the “configuration and coil currents” bar first, then this bar\'s.',
  'pulse.j.src.filenone': '★The file did not record which configuration it was written against (the bar above had not solved one at export time), so there is nothing to compare; this page currently stands on {page}.',

  'pulse.check': 'Verification',
  'pulse.nverify': 'Waypoints verified by a forward solve',
  'pulse.verify_note': '★The waypoints are <strong>stratified by phase</strong> — just after breakdown, the ramp, the flat-top, the retrace — one per phase before any phase gets a second. Spacing them evenly spends the verification budget on the easiest instant: measured, the former default two landed at 2.50 and 7.50 s with the flat-top running 1.0–8.0 s, and the two configurations were identical. <strong>The default is now 4</strong>: there are four phases, and a budget smaller than four must miss one of them (with the waypoint grid refined by phase, all four strata finally have points to draw from — see above). When target shapes are given at several instants, <strong>those instants come first within their own phase</strong>. Each point is one free-boundary solve, reporting the configuration <strong>actually obtained</strong> beside <strong>that instant\'s own</strong> target. 0 = no verification. About 0.5–1 s each.',
  'pulse.limits': 'Limits',
  'pulse.vcap': 'Channel voltage limit [V/turn] (0 = not declared)',
  'pulse.limits_note': '★Limits are <strong>reported</strong>, never silently applied: a design that cannot be driven must say so rather than come back quietly clipped and look feasible. The current limit is set in the design bar\'s “solver” panel; both bars read it.',
  'pulse.cap.i': 'Per-channel current trajectory [kA-turn].',
  'pulse.cap.v': 'Per-channel per-turn voltage [V/turn] — what the supplies have to deliver.',
  'pulse.cap.y': 'Currents induced in the passive structure (the vessel) [kA]. Empty when this device description carries no vessel elements.',
  'pulse.cap.r': 'RMS boundary-flux spread of each waypoint\'s linear design [Wb]: this says “is this configuration askable”, not “is the equilibrium solved accurately”.',
  'pulse.table': 'Per-channel demand',
  'pulse.col.ch': 'Channel',
  'pulse.col.imax': '|I|max [kA-turn]',
  'pulse.col.vmax': '|V|max [V/turn]',
  'pulse.col.cap': 'Declared limit I · V · F',
  'pulse.col.mark': 'Against',
  // --- T-D5: where a limit came from, and what to say without one -----
  'pulse.turn': 'turn',
  'pulse.cap.unknown': 'unknown',
  'pulse.cap.noprov': 'no provenance given',
  'pulse.cap.from_device': '★Limits are taken from <strong>{who}</strong>\'s device description (engineering-limits section): {prov}. The two global inputs on this page only stand in where the machine declares nothing per channel; <strong>anything undeclared is reported as「unknown」and never replaced by a default</strong> (the FR-PULSE-004 rule).',
  'pulse.cap.none': '★<strong>{who}</strong>\'s device description carries <strong>no engineering-limits section</strong> — no OH flux swing, no per-coil current or voltage ceiling, no force ceiling. That is what every「unknown」above means: <strong>not "inside the limit", but "there is no limit to compare against"</strong>. You can type a global figure into the two inputs above as a yardstick, but it is a limit you declared, not this machine\'s. The force column reads unknown on every machine for a second reason: this page does not compute coil forces, so a declared f_max would have nothing to be read against.',
  // ---------------------------------------------------------------------
  'pulse.verify': 'Forward verification',
  'pulse.col.t': 't [s]',
  'pulse.col.phase': 'Phase',
  'pulse.ph.postbd': 'just after breakdown',
  'pulse.col.target': 'Target (R₀, a, κ, δ)',
  'pulse.col.shape': 'Achieved (R₀, a, κ, δ)',
  'pulse.col.kind': 'Boundary',
  'pulse.col.gap': 'Wall gap [m]',
  'pulse.check_note': 'The configurations in this table are <strong>solved</strong>, which is a different statement from the trajectory design above: the design says what was asked for, this says what came out.',
  'pulse.needs': 'Design a configuration in the shape-and-coil-currents bar first',
  // --- T-D4: which configuration this trajectory was designed for -----
  'pulse.src.reached': '★This trajectory is designed for the <strong>configuration the shape-and-coil-currents bar solved</strong>: R₀ {r0} · a {a} · κ {k} · δ<sub>u</sub> {du} · δ<sub>l</sub> {dl}, I<sub>p</sub> {ip} kA. That configuration <strong>reached</strong> its target ({how}).',
  'pulse.src.missed': '★<strong>Target not met</strong>: this trajectory is designed for the configuration the shape-and-coil-currents bar <strong>solved</strong> — R₀ {r0} · a {a} · κ {k} · δ<sub>u</sub> {du} · δ<sub>l</sub> {dl}, I<sub>p</sub> {ip} kA — and that inverse solve <strong>did not reach</strong> its target ({how}). The supply demand below is therefore the demand of the shape this machine can make, not of the shape you asked for: to size supplies against the target shape, get the shape bar inside tolerance first.',
  'pulse.src.unknown': '★This trajectory is designed for the configuration the shape-and-coil-currents bar <strong>solved</strong>: R₀ {r0} · a {a} · κ {k} · δ<sub>u</sub> {du} · δ<sub>l</sub> {dl}, I<sub>p</sub> {ip} kA. That was a <strong>forward</strong> solve rather than an inverse one; how far it is from the target is in that bar\'s 「target vs achieved」 table.',
  'pulse.how.anneal': 'shape error {err} against a tolerance of {tol}',
  'pulse.how.rows': 'judged row by row against 「target vs achieved」',
  'pulse.done_missed': ', ★the configuration it was designed for missed its target',
  // -------------------------------------------------------------------
  'pulse.too_few': 'A trajectory needs at least two waypoints',
  'pulse.running': 'Designing the trajectory ({n} waypoints)…',
  'pulse.done': 'Trajectory designed: {n} waypoints, {nv} passive segments, peak voltage {v} V/turn{over}.',
  'pulse.done_over': ', ★{k} channels past the declared limit',
  'pulse.novessel': 'This device description carries no vessel elements, so the passive currents are identically zero — there is no eddy term in this trajectory.',
});
