id: https://fusion-yun.github.io/fyo/v0.9.g5b9d6bbb/dataset_fair
name: fyo-dataset_fair
title: fyo IDS `dataset_fair` (lifted, DD 4.1.1)
description: 'Semantic lift of IMAS DD IDS `dataset_fair` onto SpO/BFO (FYTOK-ADR-102): structures grounded
  as ICEs, time-slice AoS elements as spo:TimeSlice, leaves annotated with their spo carrier classes (Signal
  / Field / Function / QuantityValue / TimeAxis). Layout is preserved in dd_* annotations (reverse map
  ω).'
license: Apache-2.0
prefixes:
  linkml: https://w3id.org/linkml/
  fyo: https://fusion-yun.github.io/fyo/v0.9.g5b9d6bbb/
  spo: https://fusion-yun.github.io/spo/latest/
  bfo: http://purl.obolibrary.org/obo/BFO_
  iao: http://purl.obolibrary.org/obo/IAO_
default_prefix: fyo
default_range: string
imports:
- linkml:types
- spo
- fyo-core
- fyo-common
annotations:
  generated_by: dd2fyo 0.1.0
  dd_version: 4.1.1
  dd_source: IMAS DD schemas/dataset_fair/dd_dataset_fair.xsd (import ancestry)
  fyo_category: reference
  fyo_root_class: DatasetFair
  provenance: Imported from IMAS DD 4.1.1 via dd2fyo; fyo evolves independently — hand
    edits are legitimate, dd_* annotations record DD ancestry.
classes:
  DatasetFair:
    is_a: Dataset
    description: FAIR metadata related to the dataset, providing inforrmation on licensing, annotations,
      references using this dataset, versioning and validity, provenance. This IDS is using Dublin Core
      metadata standard whenever possible
    annotations:
      dd_path: dataset_fair
      fyo_category: reference
    attributes:
      ids_properties:
        range: IdsProperties
        description: Interface Data Structure properties. This element identifies the node above as an
          IDS
      identifier:
        range: string
        description: Persistent identifier allowing to cite this data in a public and persistent way,
          should be provided as HTTP URIs
        annotations:
          dd_type: constant
      replaces:
        range: string
        description: Persistent identifier referencing the previous version of this data
        annotations:
          dd_type: constant
      is_replaced_by:
        range: string
        description: Persistent identifier referencing the new version of this data (replacing the present
          version)
        annotations:
          dd_type: constant
      valid:
        range: string
        description: Date range during which the data is or was valid. Expressed as YYYY-MM-DD/YYYY-MM-DD,
          where the former (resp. latter) date is the data at which the data started (resp. ceased) to
          be valid. If the data is still valid, the slash should still be present, i.e. indicate the validity
          start date with YYYY-MM-DD/. If the data ceased being valid but there is no information on the
          validity start date, indicate /YYYY-MM-DD.
        annotations:
          dd_type: constant
      rights_holder:
        range: string
        description: The organization owning or managing rights over this data
        annotations:
          dd_type: constant
      license:
        range: string
        description: License(s) under which the data is made available (license description or, more convenient,
          publicly accessible URL pointing to the full license text)
        annotations:
          dd_type: constant
      is_referenced_by:
        range: string
        multivalued: true
        description: List of documents (e.g. publications) or datasets making use of this data entry (e.g.
          PIDs of other datasets using this data entry as input)
        annotations:
          dd_type: constant
          dd_ndim: 1
          dd_coordinate1: 1...N
          spo_class: spo:Function
          fyo_lift: field
          fyo_domain: 1...N
